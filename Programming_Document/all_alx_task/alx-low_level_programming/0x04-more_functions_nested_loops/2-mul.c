#include <stdio.h>

/**
 * mul - Fuction that multipies two integers
 *
 * @a: Fetches first argument from main.c
 * @b: Fetches second argument from mai.c
 *
 * Return: Always 0
 */
int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
	return (0);
}
